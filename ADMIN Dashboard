<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard – Online Quiz System</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .container {
            margin-top: 50px;
        }

        .card {
            margin-bottom: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background-color: #007bff;
            color: white;
        }

        .btn-custom {
            width: 100%;
            padding: 10px;
            font-size: 1.1em;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="text-center mb-4">Admin Dashboard – Online Quiz System</h2>
        <div id="message"></div>

        <!-- Add New Quiz Section -->
        <div class="card">
            <div class="card-header">
                <h3>Add New Quiz</h3>
            </div>
            <div class="card-body">
                <form action="add_quiz" method="post">
                    <div class="form-group">
                        <label for="quizTitle">Quiz Title:</label>
                        <input type="text" class="form-control" id="quizTitle" name="quizTitle" required>
                    </div>
                    <div class="form-group">
                        <label for="quizDescription">Description:</label>
                        <textarea class="form-control" id="quizDescription" name="quizDescription" rows="3" required></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary btn-custom">Add Quiz</button>
                </form>
            </div>
        </div>

        <!-- Manage Quizzes Section -->
        <div class="card">
            <div class="card-header">
                <h3>Manage Quizzes</h3>
            </div>
            <div class="card-body">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Quiz ID</th>
                            <th>Title</th>
                            <th>Description</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Dynamic quiz list generated by server-side scripting -->
                        <tr>
                            <td>1</td>
                            <td>Sample Quiz</td>
                            <td>This is a sample quiz description.</td>
                            <td>
                                <button class="btn btn-warning btn-sm">Edit</button>
                                <button class="btn btn-danger btn-sm">Delete</button>
                            </td>
                        </tr>
                        <!-- Add more quizzes dynamically -->
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <script>
        // Function to get URL parameters
        function getUrlParameter(name) {
            name = name.replace(/[[]/, '\').replace(/[]/, '\');
            var regex = new RegExp('[\\?&]' + name + '=([^&#]*)');
            var results = regex.exec(location.search);
            return results === null ? '' : decodeURIComponent(results[1].replace(/\+/g, ' '));
        }

        // Display success or error message based on URL parameter
        window.onload = function () {
            var success = getUrlParameter('success');
            var messageDiv = document.getElementById('message');
            if (success === 'true') {
                messageDiv.innerHTML = '<div class="alert alert-success">Operation completed successfully!</div>';
            } else if (success === 'false') {
                messageDiv.innerHTML = '<div class="alert alert-danger">Operation failed. Please try again.</div>';
            }
        };
    </script>
</body>

</html>
